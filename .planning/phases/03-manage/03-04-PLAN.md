---
phase: 03-manage
plan: 04
type: execute
wave: 3
depends_on:
  - 03-02
  - 03-03
files_modified: []
autonomous: false
requirements:
  - QADD-01
  - QADD-02
  - QADD-03
  - ITEM-01
  - ITEM-02
  - ITEM-03

must_haves:
  truths:
    - "User can add an item using the quick-add bar and see it appear in the list"
    - "User can edit an item and have changes persist"
    - "User can delete an item with confirmation"
    - "User can move an item by editing its room/container fields"
  artifacts:
    - path: "possession/tui/widgets/quickadd.py"
      provides: "Quick-add bar"
      contains: "class QuickAddBar"
    - path: "possession/tui/screens/edit.py"
      provides: "Edit form screen"
      contains: "class EditItemScreen"
  key_links: []
---

<objective>
Human verification of all Phase 3 manage features end-to-end.

Purpose: Confirm that quick-add, edit, delete, and move all work correctly from a real terminal session before marking Phase 3 complete.
Output: Human approval that the manage flow is complete and usable.
</objective>

<execution_context>
@/Users/paritoshpatil/.claude/get-shit-done/workflows/execute-plan.md
@/Users/paritoshpatil/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Human verify: all Phase 3 manage features end-to-end</name>
  <action>
    Pause for human verification. Run `python3 -m possession` and test all manage flows per the steps below.
  </action>
  <verify>Human confirms all 7 steps below pass.</verify>
  <done>User types "approved"</done>
  <what-built>
    Complete Phase 3 manage features:
    - Quick-add bar (press 'a', type name / description / room / container / category / date / cost, Enter to save)
    - Partial quick-add (name only, or name / room)
    - New room/container confirmation prompt during quick-add
    - Item edit form (press 'e' on selected item, Tab between fields, Ctrl+S to save, Escape to cancel)
    - Item move via edit (change room or container in the edit form)
    - Item delete with confirmation (press 'd', type 'y', Enter to confirm; Escape to cancel)
  </what-built>
  <how-to-verify>
    Run the app: `python3 -m possession`

    1. **Quick-add (QADD-01):** Press 'a'. Type `Test Lamp / Bedside reading lamp / Bedroom / Nightstand / Lighting`. Press Enter. Navigate to Bedroom > Nightstand — confirm "Test Lamp" appears.

    2. **Partial quick-add (QADD-02):** Press 'a'. Type just `Spare Key`. Press Enter. Confirm "Spare Key" appears (with no room/container/category).

    3. **New room confirmation (QADD-03):** Press 'a'. Type `Widget / / Brand New Room`. Press Enter. A prompt should appear asking to confirm creating "Brand New Room". Type 'y', Enter. Confirm item saved in new room.

    4. **Edit item (ITEM-01):** Drill to any items view. Select an item. Press 'e'. Confirm form opens pre-filled. Change the description. Press Ctrl+S. Confirm change appears in the list.

    5. **Move item (ITEM-03):** Press 'e' on an item. Change the Room field to a different room. Press Ctrl+S. Verify item appears under new room when you drill into it.

    6. **Delete item (ITEM-02):** Select an item. Press 'd'. Confirm prompt appears. Type 'y', Enter. Confirm item is gone from the list.

    7. **Delete cancel:** Select an item. Press 'd'. Press Escape. Confirm item is NOT deleted.
  </how-to-verify>
  <resume-signal>Type "approved" if all flows work correctly, or describe specific issues found</resume-signal>
</task>

</tasks>

<verification>
Human approval of all 7 verification steps above.
</verification>

<success_criteria>
All 7 manual verification steps pass. Phase 3 is complete.
</success_criteria>

<output>
After completion, create `.planning/phases/03-manage/03-04-SUMMARY.md`
</output>
