[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "possession-terminal"
version = "0.1.0"
requires-python = ">=3.9"
dependencies = [
    "textual>=0.47.0",
]

[project.scripts]
possession = "possession.__main__:main"
